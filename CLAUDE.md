# CLAUDE.md

전체 프로젝트 가이드(구조·환경·명령어·코드 스타일·검증·커밋/PR/Issue 규칙)는 [`AGENTS.md`](AGENTS.md)에 있다. 아래는 그중 **놓치면 손해가 큰 3가지**만 옮겨 둔 것이다.

- **결정 지점은 혼자 정하지 않는다.** 하이퍼파라미터, 데이터 포맷, 스키마처럼 여러 선택지가 가능한 지점은 구현 전에 가정을 명시하거나 먼저 물어본다. 이슈 체크리스트에 있는 항목이라도 예외 아니다 — "체크리스트에 있으니 진행"은 확인 질문을 건너뛰는 이유가 안 된다
- **PR·Issue·커밋처럼 공유 상태를 바꾸는 행동은 항상 사용자 승인 후에 실행한다.** PR·Issue 본문은 `.github/pull_request_template.md`, `.github/ISSUE_TEMPLATE/issue-form.md`의 섹션을 생략·재구성하지 않고 그대로 따른다
- **고정된 버전·구조는 이유가 있어서 고정된 것이다.** `torch==2.5.1+cpu`(Windows `c10.dll` 문제로 다운그레이드)처럼 버전을 임의로 올리지 않고, `Ai/`의 스크립트 스타일(패키지 아님, bare import)도 임의로 "개선"하지 않는다

사용자와의 대화, 커밋 메시지, 문서(`docs/`)는 모두 한글로 작성한다.
